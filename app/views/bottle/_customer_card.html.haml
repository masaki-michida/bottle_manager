.title__card
  -@bottle.each do |b|
    .row
      .col.s10
        .card.blue-grey.darken-1
          .card-content.white-text
            %span=b.local_number
            &nbsp;
            %span=KindOfAlchol.find(b.kind_of_alchol_id).name
            &nbsp;
            %span="#{b.liquid_level}"
            %span="#{b.id}"